<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (HTTP, not HTTPS) is permitted only for the Android emulator's
  host-loopback alias, used exclusively when the app is built with
  USE_EMULATORS=true to talk to local Firebase emulators during development.
  Every other destination keeps the platform default (HTTPS only).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
